<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.boohee.boohee">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".View.MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".View.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".View.Shop_View.Shop_Details_Activity" />
        <activity android:name=".View.Shop_View.Shop_GridView_Activity" />
        <activity android:name=".View.Shop_View.GoodsCar_Activity"/>
        <activity android:name=".View.Shop_View.DingDan_Activity"/>
    </application>

</manifest>